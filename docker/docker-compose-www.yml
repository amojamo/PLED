version: "3"
services:
  www:
      build:
          context: .
          dockerfile: ./Dockerfile.www
      ports:
          - 4444:4444
      volumes:
          - ../www:/var/www/html/
      networks:
          - default
  
